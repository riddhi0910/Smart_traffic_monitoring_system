# Core dependencies
Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0

# YOLO and Computer Vision
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=9.0.0

# PyTorch CPU-only (much smaller, works on Render free tier)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0